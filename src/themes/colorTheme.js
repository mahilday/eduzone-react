import { createMuiTheme } from "@material-ui/core";

export const colorTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#2ec4b6",
    },
    secondary: {
      main: "#f4f4f4",
    },
  },
});
